rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Browser and mobile SDKs never access commerce data directly.
    // Trusted Next.js server code uses Firebase Admin, which bypasses rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
